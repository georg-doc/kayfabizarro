# TRACK-CORE-1A · Blender geometry oracle / proof brief · 2026-09-26

**Executing agent:** **Claude Coworker + Blender MCP**  
**Owner:** Track Core remains `georg-doc/KFB-Stunt-Car-Race`; Blender is a proof/oracle consumer  
**Starts after:** TRACK-CORE-0 + Georg language decision  
**Purpose:** prove the generic geometry mechanisms visually/numerically without creating a second authoritative compiler

## Binding source order

Read:

1. current Track-Core `START_HERE.md`;
2. TRACK-CORE-0 outputs;
3. `PERPLEXITY_TRANSITION_RESEARCH_ASSESSMENT.md`;
4. Race PR #42 / RKIT-11 `RETURN.md`, `SOURCE.json`, `transition_lib.py`;
5. exact current RKIT base / stunt donors named by the Web census.

GitHub state wins.

Do not start if TRACK-CORE-0 contracts are incomplete.

---

# Role of Blender in this gate

Blender MCP is **not** the owner of a second track system.

It is used to:

- build source-exact visual proof;
- compare geometry against existing RKIT;
- test centre-line/frame and slot-profile behavior;
- produce GLB/evidence;
- act as an independent numerical/visual oracle for the authoritative core.

If the language gate chooses JavaScript as authoritative core, Blender/Python must consume/export the same contract or independently verify fixtures. It may not silently become the real solver.

---

# 1 · Do not run the Perplexity sample script as production

The sample is inspiration only.

Explicitly do not inherit:

- `clear_scene()`;
- its generic materials;
- separate road / line / barrier generators;
- simplified `current_pos.y += length` stepping;
- the circular arc labelled as “Clothoid”;
- barrier-always-on assumptions;
- alpha-fade-as-contact ideas.

Use actual KFB/RKIT donors.

---

# 2 · Source-object isolation first

Before new proof geometry, show in isolation:

- canonical current RKIT base track/profile donor;
- current CITY_STREET donor;
- current 3D stunt/loop donor;
- RKIT-11 transition v2;
- RKIT-11 bridge carriageway source;
- existing marking outputs relevant to STREET / TRACK / MAG.

Capture exact source paths/heads.

A loaded source is not proof until its actual visible design is shown.

---

# 3 · Proof A · one frame stream

Build a small isolated comparison scene from the TRACK-CORE-0 frame contract.

Prove all of these feed the same frame type:

1. straight;
2. planar curve;
3. vertical grade;
4. bank;
5. loop / 3D curve.

Visible debug helpers are allowed only in the evidence scene.

Measure:

- orthogonality of T/U/R;
- frame flips;
- arc-length monotonicity;
- endpoint frame parity.

No alternate “loop frame” structure.

---

# 4 · Proof B · actual Euler / Clothoid curvature ease

Implement a real curvature-continuous horizontal test piece.

Minimum fixture:

```text
STRAIGHT
→ CLOTHOID IN
→ TARGET CURVE
→ CLOTHOID OUT
→ STRAIGHT
```

Also derive:

- mirrored S-offset;
- chicane;
- hairpin approach + core + exit.

Evidence must include curvature over `s`.

Do not label a circular arc or Bezier-only approximation “Clothoid” unless the math/contract explicitly says it is an approximation.

The Web contract decides acceptable implementation family.

---

# 5 · Proof C · one canonical slot profile

Generate all of these from the same named slot topology:

- barrier-free road;
- CITY_STREET with curb/walkway;
- STANDARD/WIDE RKIT track;
- narrow 6 m stunt lane;
- 9.85 m bridge carriageway;
- bridge-like deck with walkway/parapet;
- full race barriers.

No profile may change vertex/slot semantics.

Hidden slots may collapse/disable according to the contract, but topology remains compatible.

Create a slot-isolation sheet with labels.

---

# 6 · Proof D · transitions are parameter interpolation

Rebuild two transitions without special geometry code:

## D1 · STREET → TRACK

Parameter curves only:

- width;
- curb/walkway;
- shoulder;
- barrier rise;
- markings;
- road/surface role.

## D2 · RKIT-11 lane takeover

Recreate the useful visual timing of `transition_lib.py`:

- width/funnel;
- barrier rise;
- line transition;
- MAG stripe appearance;
- slab/surface visibility if still required by the core contract.

Forbidden:

- monkey-patching `section_profile`;
- host marking cuts;
- rebuilding host meshes;
- special `tr` / `ts` / `bs` frame fields outside the canonical parameter-vector contract.

The resulting transition must be generated by the same core surface/marking paths as ordinary track.

---

# 7 · Proof E · markings are independent

Show one route with:

```text
STREET → blend → TRACK → blend → MAG
```

using one marking renderer/layer.

Prove:

- no host-line cutting;
- no duplicate overlapping markings;
- no obvious coplanar flicker in the Blender review;
- widths/offsets/blend weights follow the same `s` frame stream.

---

# 8 · Proof F · RKIT-11 acceptance fixture

Only after A–E work.

Rebuild enough of the frozen RKIT-11 scene to prove the core can replace its hacks:

- Mülheimer Brücke drive deck = Track-Core profile/parameters;
- Pylon Loop = piece data on the same core;
- transition/takeover = parameter curves;
- street/bridge markings = marking layer;
- no separate bridge-track sweep;
- no separate loop surface generator;
- no special host line cuts.

The authored landmark shell may remain:

- pylons;
- cables;
- portal;
- non-driveable bridge identity geometry.

Driveable surface remains one core.

No need to rebuild unapproved OSM files if source promotion is still pending. Use the pinned frozen fixture transforms if necessary and state provenance.

---

# 9 · Geometry Nodes role

Geometry Nodes may be used to visualize or manipulate:

- slot profile;
- `s` parameter curves;
- route sampling;
- prop/style scattering.

But GN is not allowed to become an unexported hidden SSOT.

All final proof facts must be recoverable from the shared contract/data or explicit scripts.

---

# 10 · Tests

Report actual counts.

At minimum:

- endpoint position delta;
- tangent delta;
- curvature continuity fixture;
- frame orthogonality;
- frame flip count;
- slot semantic parity;
- transition slot-value discontinuities;
- self intersections;
- body folds;
- marking duplicate/overlap checks where measurable;
- clearance for RKIT-11 loop acceptance scene;
- GLB reimport;
- recipe/frame fixture reproducibility.

Do not claim Race driving.

---

# 11 · Outputs

Create a bounded proof package in the appropriate Race branch/folder after Web names it.

Return:

- `RETURN.md` — defects first;
- `SOURCE.json`;
- `TEST_REPORT.md`;
- additive `CHANGELOG.md`;
- scripts/data fixtures;
- GLBs;
- isolated donor images;
- frame proof;
- curvature plot/proof;
- slot-profile atlas;
- STREET→TRACK transition views;
- lane-takeover views;
- marking transition views;
- RKIT-11 acceptance comparison.

Blender sources remain in the established source location if too large for GitHub; repo scripts/data must rebuild the proof.

---

# Do not do

- no Race physics changes;
- no controller/camera changes;
- no new palette system;
- no generic neon materials;
- no new RouteRecipe;
- no second compiler;
- no whole Cologne OSM import;
- no Claude Design UI;
- no Stage/Live promotion;
- no auto-merge.

## Done when

Blender proves the contracts can generate the required flexible connector behavior **without special-case geometry code**.

## Next gate

**TRACK-CORE-1B · Web authoritative implementation / runtime parity**, using the language decision and the Blender fixtures as an oracle.
