# Evidence

Canonical CI evidence remains in GitHub Actions artifacts.

- Run `35461123803` · artifact `10589419759` · contains `FAIL.png` + browser report.
- Run `35461231731` · artifact `10589564787` · contains `FAIL.png` + browser report.

Both screenshots show the browser never reaching the module-kit view because the Librarian ES module fails during parse.

The JSON reports are copied here as durable text evidence. The binary failure screenshots remain in the workflow artifacts; they are not re-encoded into a second repository copy.
