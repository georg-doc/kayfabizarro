# Coworker Integration Proposals

Store cross-lane proposals here.

Every proposal starts as `PROPOSAL`.
It becomes `WEB_ALIGNED` only after Web Lead / Georg review.
Only aligned proposals may be turned into WSA-ready slice briefs.
