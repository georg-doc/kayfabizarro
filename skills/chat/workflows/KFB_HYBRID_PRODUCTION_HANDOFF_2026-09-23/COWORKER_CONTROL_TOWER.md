# Claude Coworker · KFB Control Tower · 2026-09-23

Status: **OPERATIVE COORDINATION BRIEF**

## Für Georg

Coworker soll jetzt die Web-Slices praktisch verwalten, damit du nicht für jede kleine Reparatur einen neuen Web-Lead-Chat brauchst.

Der nächste Web-Lead-Check kommt erst wieder bei:
- einem Owner-Konflikt;
- einem echten Architekturentscheid;
- einem vorbereiteten WSA-Workshop;
- oder einem größeren MVP-Check-in.

## Coworker mode

Default:
**PRODUCTIVE + CHECK-IN**

Coworker may:
- implement bounded GitHub slices;
- use subagents for source extraction/recon;
- run tests;
- persist review artifacts;
- maintain compact lane status;
- prepare CLOSED Production Packets;
- return direct clickable human artifacts.

Coworker must not:
- invent donors;
- create replacement owners;
- silently widen one slice into a redesign;
- ask Georg to approve every internal checkpoint;
- promote Live or merge without the named gate.

## Current ToolBox instruction

Continue the current Source-Safe Integration 01 build.

The internal faithful vertical slice is sensible:
- real Stage-First shell;
- real roster;
- accepted R2 editor;
- Goth Girl;
- Save/Reload.

But:

**do not stop there for a Georg human gate unless blocked.**

If the vertical slice is technically/source-correct, continue to the coherent packet milestone:
- current FrizzleBob Driver Graft;
- full actual Studio roster;
- saved Cube-Pet 1.2.9 user state;
- Goth Girl;
- Orc Warband;
- Animatronic;
- Move / Rotate / Scale / Drop;
- Save / Reload;
- missing-source fail-closed behavior.

Then deliver one clickable ToolBox review.

Persist the newly verified Base-Pet source pin in ToolBox evidence:
`media/3D_Assets/kfb-pets.json`
blob:
`cef6cca9cc69a6cb3c0b4a9d39f6b15579cbf055`

## After ToolBox milestone

Do a **single current-lane check-in**, not implementation of everything.

Read:
- this handoff CURRENT_STATE;
- WorldBuilder #190;
- Travel #38;
- Racer #33;
- Blender #192 / latest Warband return if available;
- Hürth form-language PR #194;
- Curtain #189;
- Billboard #188;
- current cartoon-form-language note.

If the newest Warband work exists only in Dropbox, report it as **candidate-only / GitHub Return pending** rather than promoting Dropbox state into implementation truth.

Then return exactly:

### 1 · What is production-ready now?

Plain language.

### 2 · What remains human-gated?

Only real product/visual decisions.

### 3 · What should be the next ONE MVP?

Propose one coherent MVP, with owner split.

### 4 · What stays parallel but does not block it?

Keep side lanes out of the MVP.

### 5 · Does anything genuinely need WSA now?

If no:
say `WSA NOT YET NEEDED`.

## Preferred next MVP candidate

Do not treat this as pre-approved, but evaluate first:

**HYBRID-PROD-MVP-01 · Blender-authored Resident → ToolBox/WorldBuilder runtime**

Possible coherent proof:
1. one accepted Blender-authored Warband performance bundle;
2. source-safe ToolBox can select/load the real actors/preset;
3. existing WorldBuilder scene host can place/edit the scene;
4. shared editor changes position/rotation/scale;
5. Save/Reload survives;
6. authored animation plays through an existing runtime consumer;
7. one coherent review artifact.

This would prove the new hybrid pipeline end-to-end.

Racer Anatomy Foundation should remain a separate product lane unless Georg explicitly prioritizes it as the MVP.

## Racer rule

R3d is TUNE.

Do not start Vehicle Grounding yet.

Prepare a CLOSED `Racer Anatomy Foundation` packet before the next Racer implementation.

The packet may compare:
- procedural topology repair;
- Blender visual-mesh A/B.

No full-track Blender conversion without a representative A/B.

## Human artifact rule

Every actual Georg gate:
- direct link in chat;
- durable repo/CI storage;
- plain-language questions;
- actual source/head recorded.

## Recovery

After any Coworker context loss:
read this file + CURRENT_STATE + exactly one owner PR.


## Model recommendation

Current recommended Coworker model:
**Claude Opus 5.5**

Reason:
this control-tower role is dominated by:
- long-running source reconciliation;
- multi-tool work;
- agentic coding;
- subagent coordination;
- cross-file integration.

Switch in the same chat only after the model-switch checkpoint:
`COWORKER_OPUS55_MODEL_SWITCH.md`

Do not treat model capability as permission to widen scope.

## Performance-animation routing

When a Blender/Warband issue is not merely “cartoon timing” but a physical task such as:
- drumming;
- guitar strumming;
- juggling;
- hammering;
- prop contact;

load:
`PERFORMANCE_ANIMATION_PILOT.md`

Use contact-first mechanics + beat/event sidecar rather than repeated visual guessing.

The pilot remains candidate-only until Warband + one second action family prove it.

## WSA synchronization

Use:
`COWORKER_WSA_SYNC_BRIDGE.md`

Coworker remains the current-state writer.
WSA writes execution Returns only.
Web Lead reconciles after a meaningful milestone.
