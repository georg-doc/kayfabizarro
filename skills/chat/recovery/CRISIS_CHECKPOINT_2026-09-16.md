# KFB CRISIS CHECKPOINT · 2026-09-16

**Status:** CURRENT PROJECT CHECKPOINT · no new runtime implementation · no owner replacement.

This checkpoint exists to stop planning drift and preserve a clean restart point after the 2026-09-15/16 Birthday / ToolBox / World work.

## 1 · Current repository baseline

Repository: `georg-doc/kayfabizarro`

Checkpoint created after inbox housekeeping commit:

`c495206ba150514df65947793ed4acf18d91859c`

GitHub state overrides chat recollection.

## 2 · CURRENT · ToolBox / Actor authoring

### Technical baseline

Use the reviewed WS0 source tree:

`tools/KFB-ToolBox/_inbox/WS0_2026-09-15/`

Important tested result already recorded there:

- 7/7 authoring sheets cold-started;
- module closure passed;
- package checksums passed;
- source tree is available and does not need to be reconstructed from docs.

### Current Studio donor

`tools/KFB-ToolBox/_inbox/KFB FrankenStein Studio (7).zip`

Git blob:

`4ea1ead8919b02b7a9a09f7683d3252f548b31d9`

Upload commit:

`79c5799afd185693f245755f19de2abb44d42452`

Classification:

`CURRENT STUDIO WIP / PROMOTION DONOR`

Do not infer semantic revision order from browser suffixes `(6)` / `(7)`.

Session-reported v18 handoff includes:

- exported `profiles/kfb-pet-gothgirl.json`;
- exported `profiles/kfb-pet-hihi.json`;
- measured GothGirl appearance state;
- eleven `CANDIDATE CONTRACT DELTA` items;
- current motion proof;
- single-Pet exporter fix allowing custom/new Cube-Pet style entries to qualify through `modelId` instead of only `kind === 'biped' || module`.

The v18 findings are promotion inputs. They do NOT automatically rewrite canonical ToolBox contracts or `pet-LIBRARY.json`.

## 3 · CURRENT · ToolBox product direction

Current working UI direction:

`tools/KFB-ToolBox/_handover/UI_RESET_MINIMAL_STAGE_FIRST_2026-09-15/`

Core product shell:

`Actor · Face · Pose · Motion · Voice · Stage`

Stage stays visually dominant. One persistent navigation layer. No permanent developer/provenance dashboard in normal authoring.

`KFB ToolBox Stage First v3.zip` is a provisional design/rescue donor, not an implementation SSOT by age alone.

## 4 · Contract discipline

Keep these concepts distinct:

### `kfb.pets/1`

Studio/Actor authoring profile contract. Current exported actor profiles may use it.

### `media/3D_Assets/pet-LIBRARY.json`

Existing shared canonical Pet Library owner. Do not inject v18 WIP actor/profile fields into it merely to make a current Studio build work.

### Asset Registry / Asset Librarian

Discovery/provenance owner only. ToolBox/Animation/Rigging remain receiving suitability/compatibility owners.

No new Pet architecture should be invented during the v18 promotion pass.

## 5 · CURRENT · Asset / Visual Scene Atlas work

Active preflight:

`tools/KFB-ToolBox/_inbox/KFB Asset Librarian · Visual Scene Atlas Preflight.md`

This remains source/discovery preparation. Claude Design visual analysis should consume prepared jobs rather than re-indexing the corpus.

## 6 · CURRENT · Birthday references

Active source package:

`tools/KFB-ToolBox/_inbox/KFB Elisa B-Day Reference+Mockups/`

For Birthday composition, Georg's own mockup remains the accepted high-value visual reference unless Georg explicitly replaces it.

Do not silently promote a weaker downstream lookdev frame over it.

## 7 · REJECTED · Fable G0 Birthday visual direction

The Fable 5 G0 output in `georg-doc/KFB-Travel-Globe` is now classified:

`REJECTED VISUAL DIRECTION / TECHNICAL LOOKDEV HARNESS HISTORY`

Reason:

- it did not improve on Georg's existing Birthday mockup;
- it used a screenshot plate plus a separate overlay harness rather than producing production integration;
- several identity-bearing spatial relationships were visually wrong, missing, or unreadable;
- code-level presence is not visual acceptance;
- the harness explicitly requires later reimplementation for the new overlay elements.

Do NOT use that Hero frame as the visual target for Astra or other production integration.

It may remain as archived technical evidence only.

## 8 · Birthday visual target hierarchy

Until Georg explicitly changes it:

1. Travel runtime/world truth: `georg-doc/KFB-Travel-Globe`.
2. Birthday visual/composition target: Georg Birthday mockup/reference package.
3. Actor authoring input: current v18 donor / exported profiles.
4. Asset input: real existing KFB/KayKit/Travel assets and approved modules.
5. New authoring only where the required element truly does not yet exist.

Do not replace a real available asset with an invented primitive merely to satisfy a visual checklist.

## 9 · Parallel work allowed from this checkpoint

### ToolBox lane

Allowed next step:

`v18 PROMOTION / INTEGRATION INTO TESTED WS0 SOURCE BASELINE`

First slice only:

`Actor + Face + Motion + Export`

No full World Builder / Stage implementation before that integrated slice passes and Georg sees the result.

### World lane

No further autonomous Birthday lookdev run from the rejected Fable Hero.

Any production integration must start from the real Travel runtime plus Georg's accepted mockup/reference placement.

## 10 · Hard stop rules

From this checkpoint:

- no broad new plan before reading this file and current repo state;
- no silent owner/contract replacement;
- no automated PASS treated as Georg visual acceptance;
- no code-path existence treated as visible success;
- no new visual mockup replaces Georg's accepted reference unless it is visibly better and explicitly accepted;
- no rebuilding functioning modules from prose;
- no runtime implementation hidden inside a design/research task.

## 11 · Status summary

`CURRENT` · WS0 ToolBox source baseline.

`CURRENT WIP DONOR` · FrankenStein Studio v18 / `(7).zip`.

`CURRENT DESIGN DIRECTION` · minimal Stage-First ToolBox.

`CURRENT REFERENCE` · Georg Birthday mockup/reference package.

`REJECTED VISUAL DIRECTION` · Fable G0 Birthday Hero.

`NEXT ALLOWED TOOLBOX SLICE` · v18 promotion into tested source baseline; Actor + Face + Motion + Export only.

`NEXT WORLD ACTION` · none automatic; resume from real Travel + accepted Georg reference, not from rejected Fable Hero.
