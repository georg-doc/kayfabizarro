# Recovery

Resume only when the external/universal gizmo can export a transform, or an equivalent deterministic transform-authoring surface exists.

Start from the proven donor/socket/release chain and the public baseline `a5e09d6744f4a26e63a5f7e706be8a631459d28d`, not from either failed pitch number as a default.

On resume: record gizmo/tool revision → freeze Aim time → import one transform → apply same shared Rig_Medium profile to FB/GothGirl → one browser proof → only then Stage publication.
