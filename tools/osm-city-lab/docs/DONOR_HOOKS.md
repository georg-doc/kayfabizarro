# Deferred donor hooks

## img2threejs

Optional landmark lane only:

`OSM identity + footprint/anchor → accepted low-poly landmark → landmarkOverride`

The existing Dom prototype remains experimental and is not a dependency of Ehrenfeld v0.

## 2D Animation Studio

Optional later city-actor mechanism for KFB-authored cutouts: residents, signs, murals, flags, cardboard figures and background crowds. City consumes exported modules; the Studio remains rig/animation owner. DocCheck Eumel is not a KFB resident.
