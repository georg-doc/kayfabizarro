# Consolidated integration intent

This branch is the review/merge surface for the complete KFB Asset Librarian v1 stack.

Development PRs #2–#7 remain as slice history and should not be merged individually after this integration path is accepted.

No generated registry payload is hand-authored here. The post-merge GitHub Action must create the first reviewable `bot/asset-registry-update` PR from real `main`.
