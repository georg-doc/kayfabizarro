# KNOWN ISSUES · Hybrid Surface v2 frozen candidate

1. **Actor compile census unresolved** — five decorated actor material records are counted effectively visible but do not show an `onBeforeCompile` marker in the proof.
2. **Exact five records unknown** — current telemetry reports aggregate counts only.
3. **v2 seam quality unreviewed** — seam-soft triplanar code exists, but the proof stops before screenshot evidence.
4. **Clay/grain visual quality unreviewed** — implementation exists; no Georg acceptance.
5. **Hair/armour highlight preservation unreviewed visually** — source roughness values are preserved technically, but visual result was not reached as a human gate.
6. **Head-size calibration needs human read** — numerical relation is proven; cartoon proportion still needs visual approval.
7. **SOURCE.json frozen runtime marker predates recovery metadata** — recovery docs use `frozenRuntimeHead=7cbad52b…`; do not interpret later documentation commits as a newer tested runtime.
8. **No public v2 route** — the v1 public Stage remains the only valid human surface.
9. **No environment claim** — OSM/Race/landmark work remains downstream.
