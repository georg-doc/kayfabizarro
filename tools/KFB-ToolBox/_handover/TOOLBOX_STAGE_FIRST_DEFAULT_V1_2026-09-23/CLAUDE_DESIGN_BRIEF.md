# Claude Design · KFB ToolBox Stage-First Default · 2026-09-23

> **HOLD · DO NOT CONTINUE ROUND 1**
>
> Georg rejected the first build because it used the old Cube Bunny as FrizzleBob, lost saved Cube-Pet configuration, and did not use the full current Studio actor source.
>
> Read first:
> `tools/KFB-ToolBox/_handover/TOOLBOX_SOURCE_LOCK_2026-09-23/START_HERE.md`
>
> Do **not** repair the current Round-1 file. Preserve/export it as failed evidence.
> The next gate is source review only: real Driver Graft alone, saved Cube-Pets alone, actual Studio roster. No new consolidated UI until Georg accepts those sources.

Use this together with:
`START_HERE.md`

## Source-first requirement

Before modifying anything, open/show the actual visual source:

Dropbox:
`/CLAUDE/KFB ToolBox v0.5/KFB-ToolBox/_handover/UI_RESET_MINIMAL_STAGE_FIRST_2026-09-15/design/KFB ToolBox Stage-First Concept.dc.html`

The visual source contains the intended 1440×900, ~832 px and interaction-map compositions.

**Do not redraw the concept from this prose.**

Also load the current v0.5 `stage-first/` source from the same export.

## Authoring target

Build the candidate default KFB ToolBox shell:

`Actor ▾ · Body · Face · Motion · Voice · Messen`

one bar, dominant stage, one context palette.

Keep exact current owners:
- EyeRig v6;
- Eye batch profiles;
- FrizzleBob graft;
- Motion/rig owners;
- Resource Registry/Librarian;
- S21 detail-edit source;
- Resident/World hosts.

## Priority order

1. parity with the Stage-First Concept UI;
2. actual actor/source roster in `Actor ▾`;
3. Face/Messen consuming current EyeRig v6;
4. Legacy 17-profile review inside the ToolBox shell;
5. one working S21-style in-place transform edit;
6. neutral Stage preset;
7. only then optional micro-fixtures:
   - alternate Rig_Medium body + FrizzleBob graft;
   - vehicle FaceHost + EyeRig v6;
   - weapon/hand attachment orientation.

If an optional fixture has ambiguous source:
mark `SOURCE_REQUIRED` and continue.

## Prohibited

- no new EyeRig;
- no new motion owner;
- no replacement Resource Registry;
- no second scene editor;
- no permanent diagnostics dashboard;
- no extra navigation row;
- no generic UI redesign;
- no Cloudflare deployment work;
- no Work/WSA.

## Session Cut

Export:
- complete editable source;
- exact changed-file manifest;
- source pins;
- current UI state;
- screenshots of desktop and ~832 px;
- additive changelog;
- unresolved source requirements.

A Web chat will rehome the Session Cut and generate the zero-install `TOOLBOX_REVIEW.html`.

Do not claim final acceptance from Claude's own preview.
