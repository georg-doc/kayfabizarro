# WSA · Claymation line recon and review

Status: **RECON COMPLETE FOR TEXT / IMAGE · EXECUTION GATES OPEN**

## Evidence and priority

| Item | Present evidence | Interpretation / action |
|---|---|---|
| Main ClayBound style intake | Poster PNG, NotebookLM research and 4 PDF source files | Screenshot material language is donor evidence; the four large PDFs were registered, not decoded in this bounded recon. |
| First `ChatGPT-Bild…png` | Opened 1254² RGB presentation board | Preserve as look donor. Do not use whole file as seamless map or trust printed 4096 label. |
| PR #228 Clay pipeline | Open planning branch, `ASSET_MANIFEST.json`, Blender brief, Asset 03 recovery | Reuse. Manifest claims accepted Asset 01 r1 and QA. PNG binaries are outside PR and must be retrieved and verified before Blender. |
| Tileable Macro Seam Lab PR #173 | Existing seam/local and half-offset reference, public 17/17 in previous Return | Reuse test logic instead of a second seam architecture. |
| Existing game owners | ToolBox → WorldBuilder local host → Race Track Core | Three sequential bounded consumer PRs, not a global look replacement. |

## WSA review decisions requested

1. Confirm exact retrievable destination of Asset 01 r1 and its QA preview; hash-check bytes from PR #228's manifest. Do not confuse with first poster.
2. Pick one current source model/prop and one material zone for CLAY-B0/B1. If unavailable, neutral shader proof may proceed but cannot count as KFB integration.
3. Resolve whether PR #228 keeps PDF/source links intact before merge. A branch diff against main currently shows many `_inbox` source files as deleted; their loss must be reviewed separately, never accepted as incidental cleanup.
4. After ToolBox human look PASS, authorize one WorldBuilder static patch. Race only after that patch and current Track Core source contract are rechecked.

## Renderer and animation boundaries

Cycles-only Bevel shading and Random Walk stay in look-dev. Eevee uses measured approximation. Runtime gets explicit baked export-safe maps. Height/roughness are independent of color. Stable UV/tangent detail on deforming rigs; static triplanar is a separate option. Rig binding, Eyes/EyeRig, movement, lighting/sky, terrain and Race physics retain their owners.

**One next gate:** Asset 01 r1 byte+hash recovery and isolated source-object selection for Blender; no Asset 03 promotion.
