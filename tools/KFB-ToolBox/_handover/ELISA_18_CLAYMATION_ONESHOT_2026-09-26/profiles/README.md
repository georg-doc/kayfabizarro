# FrizzleBob one-shot profile drop

Place Georg's new Studio export here with this exact filename:

`frizzlebob.georg-2026-09-26.json`

The Elisa 18 autonomous one-shot treats that JSON as its frozen FrizzleBob input.

Do not place an older fallback profile here and do not rename another profile to satisfy the contract.

This folder is an execution handover only; adding the JSON here does not automatically promote it to global FrizzleBob canon.
