# START HERE · KFB ToolBox · Claude Design Session Cut · 2026-09-25 r1

Open `KFB ToolBox Production-01.dc.html` over HTTP (the folder must be served, not file://) — it loads `./support.js` and `./kfb-lib/pet-library.v6.js` relatively; everything else is pinned remote.

1. Read `HANDOVER.md` (one Next Gate), then `CURRENT_STATE.md`.
2. `RETURN.md` = TOOLBOX-PRODUCTION-01 return. `TEST_REPORT.md` = what really ran.
3. In the tool: "…" → *Run PRODUCTION-01 self-test* (13 steps, real sources, restores your save).
