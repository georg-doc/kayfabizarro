# KFB World · Opus Reconciliation Pass
## UPDATE THE EXISTING SETUP — DO NOT START OVER

Use the existing setup artifact as your working baseline:

`georg-doc/kayfabizarro/tools/KFB-ToolBox/_inbox/KFB World Design · Setup Pass.dc.html`

Do NOT recreate the World Design project from scratch.

The setup is structurally strong, but several source-status statements became stale after it was produced.

Your job is to reconcile it against current GitHub `main`, update only what changed, preserve all still-valid reasoning, and leave one clean Fable G0 launch point.

GitHub current state overrides the setup artifact wherever they conflict.

---

## 1. VERIFY CURRENT MAIN FIRST

Verify the current `georg-doc/kayfabizarro` main HEAD before editing your setup.

Recent important changes after the setup's earlier source snapshot include:

- KayKit Reference Atlas merged through PR #26;
- Cozy Look / ITHappy / Fly-Brain research merged through PR #27;
- ITHappy Licence Guard added;
- WSA Lead Intake added;
- FrankenStein Studio v18 WIP uploaded;
- current KFB World Design Setup artifact uploaded.

Do not use an older remembered HEAD as current truth.

---

## 2. CORRECT THE KAYKIT ATLAS STATUS

The existing setup currently treats the KayKit Reference Atlas as branch-only / not merged.

That is stale.

PR #26 is merged to `main`.

Canonical source:

`tools/asset_registry/librarian/_handover/KAYKIT_REFERENCE_ATLAS_2026-09-15/`

Read at minimum:

- `KAYKIT_REFERENCE_ATLAS_v1.md`
- `KAYKIT_PACK_COVERAGE_MATRIX.md`
- `KAYKIT_DEMO_SCENE_LEARNINGS.md`
- `KAYKIT_TOOLBOX_RESOURCE_PICKER_MAPPING.md`
- `BIRTHDAY_STARTER_BUNDLE_REVIEW_2026-09-15.md`

Update:

- SOURCE_MAP Atlas entry;
- any `not merged` wording;
- Atlas promotion/reconciliation handoff slot;
- final setup summary claiming no Atlas merge occurred.

Do not promote Atlas into a runtime or compatibility owner.

Asset Librarian remains discovery/reference.

Receiving consumers still own actual suitability.

---

## 3. USE THE NEW BIRTHDAY STARTER FINDING

Important current Librarian direction:

`rich discovery pool → thin Birthday scene starter → consumer validation`

The broad `kfb.asset-handoff.v1` is a discovery palette, not the final Birthday scene definition.

Do not bring every candidate prop or alternate actor into G0.

For the Birthday composition, distinguish:

### CORE
Only what is needed to establish the accepted slice.

### SIGNATURE / DRESSING
Small curated prop set with explicit scene roles.

### ALT CAST / DISCOVERY
Remain discoverable, not required scene payload.

For GothGirl, strong same-collection candidates include:

- microphone;
- mic stand;
- speaker;
- stool.

Their same-collection relationship is source-backed.

Their exact attachment/contact/performance suitability still requires consumer validation.

Do not replace the already tested Birthday motion evidence with the broad asset handoff.

---

## 4. ADD THE SOURCE-DERIVED KAYKIT COMPOSITION KNOWLEDGE

Use the Atlas as composition knowledge rather than as a random asset catalogue.

Important example:

`Forest Nature ↔ Medieval Village`

is source-author-demonstrated cross-pack composition.

This supports the KFB direction:

- authored clusters;
- companion-pack suggestions;
- coherent building/path/fence/tree/rock combinations;
- less random prop scatter.

Keep `same_collection`, `official_companion_pack`, `official_motion_companion`, `reference_demo` and `tested_kfb_preview` conceptually distinct.

None of them alone means runtime compatibility.

---

## 5. CORRECT FRANKENSTEIN STUDIO v18 STATUS

The current setup still says:

`ACTOR INPUT UPDATE PENDING · FrankenStein Studio v18`

That is stale.

Current source now exists:

`tools/KFB-ToolBox/_inbox/KFB FrankenStein Studio v18.zip`

Classify it as:

`CURRENT STUDIO WIP / SOURCE DONOR · Birthday consumer return pending`

It is NOT the new ToolBox SSOT.

Current reported v18 work includes:

- Hihi `pet.flatSkin` material path;
- per-real-part original-vs-overlay controls via `pet.orig`;
- finer mouth placement including Offset X and Roll;
- expanded overlay placement;
- 17 independently authorable GothGirl material zones;
- material-group visibility fix;
- explicit ToolBox contract deltas.

A separate v18 Birthday Consumer Return is being produced in parallel.

Fable does not wait for it to design the world.

Fable may use v18 as current actor-look / scale / material / face direction where available.

Astra later receives the explicit Consumer Return.

Update the corresponding handoff slot from:

`EMPTY`

to:

`SOURCE AVAILABLE · CONSUMER RETURN PENDING`

---

## 6. REMOVE THE LOST-DRAWING BLOCKER

The existing setup still expects a future Georg world/setup drawing.

Do not wait for it.

The original drawing is unavailable.

The current active visual/compositional input is:

`tools/KFB-ToolBox/_inbox/KFB Elisa B-Day Reference+Mockups/`

Update OD-03 and the related handoff slot accordingly.

Correct status:

`REFERENCE-CONSTRAINED PROVISIONAL GEOGRAPHY`

Meaning:

- Travel/TinySkies still owns the world substrate;
- the current Georg-authored mockup constrains recognizable composition and spatial relationships;
- Fable may improve the terrain silhouette, coast, camera, elevation, spacing, paths and staging;
- those choices do not become permanent Town canon merely because they appear in G0.

The missing original drawing is no longer a blocker.

---

## 7. CURRENT WORLD PRECEDENCE

Preserve this hierarchy:

1. `georg-doc/KFB-Travel-Globe`
   = implementation/runtime owner.

2. pinned TinySkies source
   = upstream world/environment donor.

3. Town/Birthday decisions
   = product/world intent.

4. Georg's current Birthday reference/mockup package
   = active event-staging/composition input.

5. KayKit Reference Atlas
   = asset/context/composition discovery.

6. generated mockups/color references
   = secondary LookDev only.

7. external technical donors
   = mechanisms, never hidden new owners.

Do not reverse this hierarchy.

In particular:

the mockup does NOT define terrain implementation;

TinySkies does NOT become a second runtime;

KayKit demos do NOT define Town geography.

---

## 8. COZY-LIT · USEFUL, NOT A NEW BLOCKER

Current merged research contains a concrete `cozy-lit` proposal.

Use it as a LookDev/calibration input for the desired:

`cozy / claymation substrate + KFB skew`

Useful starting characteristics include:

- high roughness;
- restrained metalness;
- broad warm key;
- strong cool/neutral fill;
- soft contact shadows;
- readable unlit sides;
- selective low-opacity contour/crease treatment;
- atmospheric depth/fog;
- compressed contrast rather than crushed blacks;
- broad silhouettes and clustered dressing.

Do NOT:

- build a watercolor → Meshy pipeline;
- replace Travel's light ownership;
- hide bad material response under global grading;
- make a new renderer just to implement `cozy-lit`.

The proposed tiny A/B/C calibration stage is useful evidence, but Elisa's Birthday is tomorrow.

Therefore:

use the research to improve Fable G0 now;

do not make a separate full cozy-lit engineering experiment a prerequisite for G0.

---

## 9. ITHAPPY · BIGGER-PICTURE DONOR, NOT BIRTHDAY SCOPE

Read:

`tools/KFB-ToolBox/_handover/ELISA_PRIORITY_STAGE_BUILDER_2026-09-15/ITHAPPY_LICENSE_GUARD_2026-09-15.md`

and:

`skills/chat/masterplan/WSA_LEAD_INTAKE_ASSET_ATLAS_COZY_FLYBRAIN_2026-09-15.md`

Record ITHappy in the Living World / ToolBox bigger-picture backlog as a potential:

`LICENSED SOURCE · modular actor/part donor family`

Keep these categories separate:

`source asset`
`modified internal working asset`
`embedded runtime asset`
`publicly exportable asset`

Do not introduce ITHappy into the current Birthday scene merely because it is available.

Do not feed ITHappy licensed asset content into generative-AI asset-processing workflows.

Public ToolBox export of ITHappy-derived reusable geometry remains a future licence-review gate.

---

## 10. FLY-BRAIN · BACKLOG ONLY

Keep Fly-Brain / FlySim as a separate experimental lane.

Do not add it to Birthday, Fable G0, Travel World implementation or current Stage work.

Potential long-term reusable idea:

`sensory inputs → internal drives → behavior adapter`

may later become KFB-owned architecture.

No current production dependency.

---

## 11. CLOSE OR REWRITE STALE OPEN DECISIONS

Audit every existing OPEN_DECISION in the setup.

Do not keep a question open merely because it was open when the artifact was generated.

At minimum revisit:

### OD-01 · sphere vs diorama

Do not frame this as two competing runtime truths.

Current governing direction:

the Start Zone belongs to the Travel world substrate.

It may be COMPOSED and CAMERA-STAGED like a readable diorama/local plateau.

That does not make it a separate flat-world owner.

Prefer:

`Travel globe / world truth + local authored Start-Zone composition`

unless current implementation evidence proves a different integration is required.

### OD-02 · G0 renderer

Choose an honest G0 route before Fable builds.

The G0 result must use real Three.js rendering and real/pinned assets.

If Claude Design uses a design-side LookDev harness rather than the production Travel host, label it explicitly:

`DESIGN LOOKDEV HARNESS`

and require:

- same source assets;
- pinned revisions;
- Travel-derived world/light parameters;
- exported camera/light/material/world config;
- explicit downstream `REIMPLEMENTATION REQUIRED` where production code is not directly transferable.

Do not present a concept render or image-model image as runtime evidence.

### OD-03 · missing setup drawing

Close/rewrite as described above.

Current mockup package replaces the unavailable original drawing as active composition input.

---

## 12. CREATIVE FREEDOM REMAINS HARD REQUIREMENT

Do not turn this reconciliation into a compliance straitjacket.

Fable is expected to DESIGN.

Within owner/identity constraints Fable may substantially improve:

- terrain silhouette;
- coastline;
- clearing shape;
- elevation;
- camera;
- focal length;
- foreground framing;
- path layout;
- prop clustering;
- lighthouse prominence;
- cloud groupings;
- rainbow placement;
- scale relationships;
- selective deformation;
- material wear;
- local colour;
- atmosphere;
- visual hierarchy.

A better integrated solution should beat literal tracing.

Hard contracts protect ownership and identity.

They do not prescribe pixels.

---

## 13. OUTPUT OF THIS RECONCILIATION PASS

Update the existing Setup artifact additively.

Do not produce a second competing setup.

Return:

1. corrected `SOURCE_MAP`;
2. corrected `OPEN_DECISIONS`;
3. corrected `HANDOFF_SLOTS`;
4. additive `LIVING_WORLD_MASTERPLAN` delta;
5. one compact revised `FABLE_G0_LAUNCH_BRIEF`.

Also return a tiny change log:

`stale statement → current fact → setup change`

Do not start Astra.

Do not build the ToolBox.

Do not broaden Birthday P0 with the whole Atlas.

Do not wait for v18 Consumer Return to begin World G0.

When reconciliation is complete, STOP with the project ready for Fable.