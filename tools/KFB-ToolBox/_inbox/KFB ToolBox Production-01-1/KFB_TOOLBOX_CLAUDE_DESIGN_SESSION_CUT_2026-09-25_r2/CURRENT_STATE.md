# CURRENT STATE · 2026-09-25 r2

- Visible: Animation Lab tab, actor FrizzleBob · Ear Rig v5, stage Neutral floor, Lab › Ears (last Georg-tab state after the EAR-DANGLE-01 run).
- localStorage `kfb-toolbox-production-01`: Georg's own save untouched by the self-test (backed up/restored). Workspace dirty in Georg's tab (unsaved) — ear review + locomotion set are in RAM only until Save.
- RUNTIME_STATE_NOT_SERIALIZED: measured locomotion set (RAM; exported copy in data/), last EAR review rows (RAM; values in TEST_REPORT), solver gate (not run in Georg's tab).
- Props (Tweaks): gridStep 0.05 · showGrid true · showContactBands true.
