# Resident Scene Module Candidates

Prepared inputs that are **not** in `tools/resident_atlas/modules/index.json` yet.

Current candidate:

- `eumel-doccheck-project-island.module.json`

Reason it is not indexed/promoted yet:

- the 2D actor source/rig exists;
- the world-space `three2p5d` renderer adapter is not yet browser-proven;
- no Resident Atlas consumer mount has been tested;
- Georg visual acceptance is pending.

The existing Resident Scene Module/Clown gate remains unchanged.
