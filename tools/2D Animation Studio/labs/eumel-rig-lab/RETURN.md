# Eumel Rig Lab · RETURN

Updated: 2026-09-18  
Status: IMPLEMENTATION · BROWSER QA PENDING

## What now exists

- source-exact visible vector split from DocCheck AD Illustrator/PDF content;
- source component manifest and semantic alias candidate map;
- separate rig contract with derived pivots;
- source PDF render alias using the same Git blob;
- browser Rig Lab implementation with 5 motion presets;
- component atlas and layer toggles.

## Important evidence boundary

The visible SVG geometry is derived from the source PDF vector operators and preserves their coordinates, transforms and stroke families. No source path has been simplified/retraced for the component split.

Rig pivots and semantic left/right aliases are separate metadata. They are not claimed to be native Illustrator anchors unless backed by a source transform.

The foreground form semantic role remains unresolved.

## Next gate

1. real browser playback;
2. original PDF render vs extracted SVG visual comparison;
3. verify eye/pupil occlusion behavior;
4. verify leg pivot directions and Hampelmann motion;
5. verify stethoscope follow-through;
6. then Georg/AD acceptance and canonical module promotion.
