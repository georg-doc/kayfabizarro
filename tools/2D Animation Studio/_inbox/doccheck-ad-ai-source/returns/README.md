# Returns

Coordination return pointers belong here after intake/audit.

Implementation and accepted canonical assets belong in the receiving Studio locations, not permanently in the inbox.

A return should state: source revision, what was inspected, what was accepted, what remains unresolved, test evidence, destination paths and the next gate.
