name: ToolBox Hybrid Surface Scene Lab proof
on:
  pull_request:
    paths:
      - 'kfb-hub/stage/toolbox/hybrid-surface-scene-lab/**'
      - '.github/workflows/toolbox-hybrid-surface-scene-lab.yml'
permissions:
  contents: read
jobs:
  browser-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Static syntax and manifest
        run: |
          node --check kfb-hub/stage/toolbox/hybrid-surface-scene-lab/hybrid-surface.v1.js
          node --check kfb-hub/stage/toolbox/hybrid-surface-scene-lab/lab.mjs
          node --check kfb-hub/stage/toolbox/hybrid-surface-scene-lab/proof.mjs
          python3 -m json.tool kfb-hub/stage/toolbox/hybrid-surface-scene-lab/SOURCE.json >/dev/null
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.51.1
          npx playwright install --with-deps chromium
      - name: Serve candidate
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/hybrid-http.log 2>&1 &
          for i in {1..30}; do curl -fsS http://127.0.0.1:4173/kfb-hub/stage/toolbox/hybrid-surface-scene-lab/ >/dev/null && exit 0; sleep 1; done
          cat /tmp/hybrid-http.log
          exit 1
      - name: Browser proof
        env:
          HYBRID_BASE_URL: http://127.0.0.1:4173/kfb-hub/stage/toolbox/hybrid-surface-scene-lab/
          HYBRID_PROOF_DIR: hybrid-surface-scene-evidence
        run: node kfb-hub/stage/toolbox/hybrid-surface-scene-lab/proof.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: toolbox-hybrid-surface-scene-evidence
          path: hybrid-surface-scene-evidence
          retention-days: 14
