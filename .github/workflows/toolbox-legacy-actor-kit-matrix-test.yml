name: ToolBox Legacy Actor Kit KLR-KIT Matrix
on:
  push:
    branches:
      - chatgpt-web/klr-kit-matrix-2026-09-21
    paths:
      - 'tools/KFB-ToolBox/legacy-rpg-rigging/**'
      - 'kfb-hub/stage/toolbox/legacy-rpg-rigging/**'
      - '.github/workflows/toolbox-legacy-actor-kit-matrix-test.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  recipe-matrix:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            tools/KFB-ToolBox/legacy-rpg-rigging
            kfb-hub/stage/toolbox/legacy-rpg-rigging
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Static + recipe + selector contracts
        run: |
          node --experimental-vm-modules tools/KFB-ToolBox/legacy-rpg-rigging/tests/static-check.mjs
          node tools/KFB-ToolBox/legacy-rpg-rigging/tests/actor-recipe.test.mjs
          node tools/KFB-ToolBox/legacy-rpg-rigging/tests/selector-regression.test.mjs
          node --check tools/KFB-ToolBox/legacy-rpg-rigging/app.js
          node --check kfb-hub/stage/toolbox/legacy-rpg-rigging/app.js
          node --check tools/KFB-ToolBox/legacy-rpg-rigging/kit-proof.mjs
      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.51.1
          npx playwright install --with-deps chromium
      - name: Start local Stage mirror
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/legacy-kit-matrix-http.log 2>&1 &
          echo $! > /tmp/legacy-kit-matrix-http.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/kfb-hub/stage/toolbox/legacy-rpg-rigging/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/legacy-kit-matrix-http.log
          exit 1
      - name: KLR-KIT three-seed browser matrix
        env:
          KLR_KIT_PROOF_DIR: klr-kit-matrix-evidence
        run: node tools/KFB-ToolBox/legacy-rpg-rigging/kit-proof.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: klr-kit-matrix-evidence
          path: klr-kit-matrix-evidence
          retention-days: 14
